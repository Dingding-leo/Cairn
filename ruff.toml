target-version = "py311"
line-length = 100
src = ["src", "tests"]

[lint]
select = ["E", "F", "B", "UP", "RUF"]
ignore = [
  "E501",  # prose-heavy validation messages are clearer unsplit
  "F401",  # package/test fixture imports may be intentional during publication port
]
