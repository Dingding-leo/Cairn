# Active chat-paper procedure

Use `paper-journal/TASK.md` and `scripts/cairn_chat_round.py` for the owner's
approved GITHUB_CHAT_PAPER_JOURNAL mode. Pull source through the available GitHub
connector when direct git networking is unavailable, execute in the current
session, append one complete decision/review record, and verify readback.

The older local-runtime host/guardian gate applies only to LOCAL_PAPER_LEDGER;
it must not block journal observations or decision recording. No TRIGGERcmd,
exchange demo account, live keys, GitHub Actions, or paid executor is required
or authorized for this journal. No account reset is permitted. Unknown account
state is preserved, not filled with 500 cash. No trade can override retained
risk limits or unavailable market evidence. Observation-based stops are not
continuous protection. Never silently switch an actual local-runtime account
into journal mode or fabricate a checkpoint.

Do not modify any scheduled task during a round. These source files do not
change native task prompts or enable paused tasks. Record active state as
UNVERIFIED unless actual native task tools establish it. A source commit, unit
test, or manual decision record does not prove recurring execution or alpha.

The owner subsequently authorized simulated leverage with a 100x ceiling. Apply the v2 borrowing, financing, equity and liquidation model in README.md; preserve all other risk gates and existing history.
